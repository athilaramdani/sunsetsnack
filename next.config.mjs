/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'cdn.pixabay.com', 'i.pinimg.com','instagram.fbdo4-1.fna.fbcdn.net','firebasestorage.googleapis.com']
    }
};

export default nextConfig;
